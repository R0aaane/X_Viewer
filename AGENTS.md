# AGENTS.md

## Purpose
このリポジトリでは、Codex / エージェントのトークン消費とレート制限を抑えるため、調査範囲・出力範囲・会話量を最小化して作業すること。

---

## Workspace rules
- すべての Flutter コマンドは `pubspec.yaml` があるプロジェクトルートで実行すること。
- `flutter.bat` の絶対パスや SDK 内バイナリを直接指定して実行しないこと。
- コマンド実行前に、必ず現在の作業ディレクトリに `pubspec.yaml` が存在することを確認すること。
- プロジェクトルート以外で Flutter / Dart / build_runner / test / pub コマンドを実行しないこと。

---

## Standard commands
- テストは必ずコマンドで実行すること。
- 修正後は、必要最小限の確認コマンドを自分で実行して確認すること。
- 不要なフルビルドや全件再解析は避け、影響範囲に応じた最小限のコマンド実行を優先すること。

---

## Before running commands
コマンド実行前に必ず以下を行うこと。

1. 現在の作業ディレクトリを表示する  
2. `pubspec.yaml` の存在を確認する  
3. 実行するコマンドがプロジェクトルート前提で問題ないことを確認する  

例:
- `pwd`
- `ls`
- `test -f pubspec.yaml` または同等の確認

---

## Token / rate limit saving rules
- 会話は最小限にすること。
- 長い説明は避けること。
- 出力は簡潔にし、必要なことだけを書くこと。
- まず修正を優先し、説明は必要最小限に留めること。
- 毎回ワークスペース全体を読むのではなく、対象ファイルを起点に必要な範囲だけ読むこと。
- 既知のプロジェクト構成や前提を毎回長く再説明しないこと。
- 変更に無関係なファイルは読まない・触らないこと。
- 全文出力より差分ベースの出力を優先すること。

---

## Reading strategy
調査時は以下の順序を守ること。

1. まずユーザーが指定したファイルだけ読む  
2. 必要なら import / 参照先を最小限だけ辿る  
3. それでも不足する場合のみ検索範囲を広げる  
4. 最後まで必要がなければ、ワークスペース全体探索はしない  

- 「とりあえず全体を読む」ことを禁止する。
- 原因調査でも、まずは責務の近いファイルから確認すること。
- 同一問題に無関係な UI / DB / server 側まで一気に広げないこと。

---

## Editing policy
- 修正は常に最小差分で行うこと。
- 既存の設計・命名・UI 方針をできるだけ維持すること。
- ユーザーから明示されていないリファクタリングを勝手に広げないこと。
- 1 回の作業では 1 目的に集中し、別問題を同時に直そうとしないこと。
- 無関係な整形・命名変更・import 並び替えをむやみに行わないこと。
- 既存コメントや既存仕様を壊す変更は避けること。
- アプリケーションのバージョンを毎回更新すること。
- webアプリケーションも同様にバージョンを更新すること。

## Encoding policy
- すべてのテキストファイルは UTF-8 として読み書きすること。
- Shift_JIS / CP932 / ANSI / Windows 既定エンコードで保存しないこと。
- 日本語のコメント、UI 文字列、ファイル名、ドキュメント本文を文字化けさせないこと。
- 既存の日本語文字列は、明示的な指示がない限り変更しないこと。
- 文字化けしているように見える箇所を見つけた場合、勝手に広範囲を自動変換せず、対象ファイルと該当箇所を報告すること。
- ファイル全体を再保存して無関係な差分を大量発生させないこと。
- 小さな修正では、対象箇所だけを最小差分で編集すること。

## Script encoding policy
- Python でファイルを読み書きする場合は、必ず `encoding="utf-8"` を明示すること。
- `Path.read_text()` / `Path.write_text()` を使う場合も、必ず `encoding="utf-8"` を指定すること。
- Windows の既定エンコードに依存しないこと。
- PowerShell / bat / cmd / Python スクリプトで日本語を出力する場合、文字化けしやすい長文ログを避けること。
- 日本語ログが必要な場合でも、確認に必要な最小限の出力に留めること。
---

## Output policy
原則として、返答は次の形を優先すること。

1. 変更ファイル  
2. 修正内容  
3. 確認手順  

- 長い経緯説明は不要。
- 全文貼り付けは避け、必要箇所のみ示すこと。
- 可能な限り差分・関数単位・変更点要約で返すこと。
- ユーザーが説明を求めた場合のみ、追加で詳しく説明すること。

---

## Investigation policy
問題調査は次の 2 段階を基本とすること。

### 1. 調査フェーズ
- まず原因候補を絞る
- 影響範囲を最小限で把握する
- この時点では無駄に大規模修正しない

### 2. 修正フェーズ
- 調査結果に基づいて最小差分で修正する
- 修正後に必要最小限の確認を行う

---

## Flutter project entry points
このプロジェクトでは、まず以下の責務に応じた入口ファイルを優先して確認すること。

新しい問題を調査する際も、まず責務が最も近いファイルから入ること。

---

## Command execution policy
- コマンドを実行する前に、何を確認するためのコマンドかを自分で明確にすること。
- 失敗時は、すぐに広範囲な再実行をせず、原因に応じて対象を絞ること。
- `flutter test`、`dart test`、`flutter analyze` などは、必要性と影響範囲を踏まえて使い分けること。
- ビルド系コマンドは重いため、必要な場合のみ実行すること。
- コマンド乱発でログとトークンを増やさないこと。

---

## Prohibited behavior
- ルート確認なしで Flutter コマンドを実行すること
- SDK の絶対パスを直接叩くこと
- 無関係なファイルまで大量に読むこと
- 無断で大規模リファクタリングすること
- 不要な長文説明を返すこと
- ユーザーが求めていない全文コードを大量出力すること
- 修正対象と無関係な箇所まで整形すること
- Shift_JIS / CP932 / ANSI / Windows 既定エンコードでファイルを保存すること
- 日本語コメント・日本語 UI 文字列を文字化けさせたまま修正を完了扱いにすること
- 文字化け対策として、無断でファイル全体を一括変換・一括再保存すること
---

## Expected style
- 簡潔
- 最小差分
- 必要箇所のみ調査
- 必要箇所のみ修正
- 必要最小限の説明
- 確認可能な手順を添える
